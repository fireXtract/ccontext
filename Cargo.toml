[package]
name = "ccontext"
version = "0.1.0"
edition = "2024"

[dependencies]
clipboard-win = ">=5.4.0"
clap = ">=4.5.43"

[profile.release]
codegen-units = 1
lto = true
opt-level = "s"
strip = true
panic = "abort"
